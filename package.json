{
  "name": "kayaba",
  "version": "1.0.0",
  "description": "Higa developpement production bot",
  "main": "build",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && npm run run",
    "run": "node .",
    "dev": "nodemon --watch ./ --ext ts --exec npm run dev:serve",
    "dev:serve": "ts-node --files index.ts -- true"
  },
  "author": "fantomitechno",
  "license": "ISC",
  "private": "true",
  "engines": {
    "node": ">=16.0.0"
  },
  "devDependencies": {
    "discord-api-types": "^0.26.1",
    "nodemon": "^2.0.14",
    "ts-node": "^10.4.0",
    "typescript": "^4.3.5"
  },
  "dependencies": {
    "higa": "^0.0.1",
    "dotenv": "^14.2.0"
  }
}
